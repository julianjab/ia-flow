node_modules/
dist/
.env
*.local
bun.lockb
.DS_Store
apps/server/logs/
apps/server/src/logs/
apps/server/config/providers.json
apps/server/config/system-prompt.md
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.playwright-mcp/
.clone/
# npm lockfiles are forbidden — bun is the only package manager
apps/**/package-lock.json
packages/**/package-lock.json

# Instancias reales de apps/agent-runner / apps/ai-provider-gateway — 4 YAML
# de config + .env por cada una, no se versionan acá (ver
# apps/agent-runner/README.md, sección "Crear una instancia nueva").
/runners/
