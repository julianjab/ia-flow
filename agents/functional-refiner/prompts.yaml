# Fuente estática para `promptRepo` en modo IA_FLOW_REPO_SOURCE=yaml (ver
# apps/server/src/infrastructure/db/yaml/YamlPromptRepository.ts).
#
# Vacío = el server usa los defaults (DEFAULT_PROVIDER_CONFIG,
# DEFAULT_ANTHROPIC_SETTINGS de @ia-flow/ai-providers) — ver
# apps/server/src/application/provider-config.ts, `getProviderConfigBlob()
# ?? {}`. Agregá `providerConfig` acá solo si necesitás overridear el
# modelo/thinking/maxTokens default para este deploy sin tocar el agente en
# agents.refiner.github-issues.yaml (que también puede overridear vía su
# propio `providerConfig` inline — ese gana si ambos setean lo mismo).
{}
