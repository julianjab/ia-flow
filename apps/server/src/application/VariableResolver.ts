// Re-export from original location — same content, published via application layer
export { resolveVariables } from '../agents/variable-resolver.js'
export type { ResolveContext } from '../agents/variable-resolver.js'
